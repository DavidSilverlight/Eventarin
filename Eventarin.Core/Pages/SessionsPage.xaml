<?xml version="1.0" encoding="UTF-8"?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:pages="clr-namespace:Eventarin.Core.Pages;assembly=Eventarin.Core"
			 xmlns:views="clr-namespace:Eventarin.Core.Views;assembly=Eventarin.Core"
		     x:Class="Eventarin.Core.Pages.SessionsPage"
		     Title="{Binding PageTitle}"
		     BackgroundColor="#E1E1E1">
	<ContentPage.Content>
		<StackLayout VerticalOptions="FillAndExpand">
			<views:PullToRefreshListView HasUnevenRows="true" ItemsSource="{Binding Sessions}" IsRefreshing="{Binding IsBusy}" RefreshCommand="{Binding RefreshCommand}" Message="Loading...">
				<ListView.ItemTemplate>
					<DataTemplate>
						<views:EventarinCell>
							<StackLayout Orientation="Horizontal" HeightRequest="80" BackgroundColor="Transparent" HorizontalOptions="Center">
								<StackLayout Orientation="Horizontal" BackgroundColor="White" Padding="10" HorizontalOptions="EndAndExpand">
									<StackLayout Orientation="Vertical" WidthRequest="250">
										<Label YAlign="Center" TextColor="Blue" Text="{Binding Title}" LineBreakMode="NoWrap" x:Name="title"></Label>
										<Label YAlign="Start" TextColor="Black" Text="{Binding Summary}" Font="12"></Label>
										<Label YAlign="Center" TextColor="Black" Font="10" Text="{Binding Location}"></Label>
									</StackLayout>
									<Button BackgroundColor="Transparent" Image="{Binding FavoriteImage}"></Button>
								</StackLayout>
							</StackLayout>
						</views:EventarinCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</views:PullToRefreshListView>
		</StackLayout>
	</ContentPage.Content>
</pages:BaseContentPage>
