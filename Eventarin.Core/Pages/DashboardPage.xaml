<?xml version="1.0" encoding="UTF-8"?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
		     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:pages="clr-namespace:Eventarin.Core.Pages;assembly=Eventarin.Core"
			 xmlns:views="clr-namespace:Eventarin.Core.Views;assembly=Eventarin.Core"
			 xmlns:converters="clr-namespace:Eventarin.Core.Converters;assembly=Eventarin.Core"
                xmlns:CustomCell="clr-namespace:Eventarin.Core.Pages;assembly=Eventarin.Core"
		      x:Class="Eventarin.Core.Pages.DashboardPage"
		     Title="{Binding PageTitle}"
		     BackgroundColor="#E1E1E1">
     <ContentPage.Resources>
          <ResourceDictionary>
               <converters:OppositeConverter x:Key="oppositeConverter" />
          </ResourceDictionary>
     </ContentPage.Resources>
     <ContentPage.Content>

          <StackLayout Orientation="Vertical"   BackgroundColor="White" VerticalOptions="FillAndExpand"  >
               <Label TextColor="Blue"  Font="10" Text="Favorite Sessions" />
               <Grid VerticalOptions="FillAndExpand" HorizontalOptions="CenterAndExpand">
                    <Grid.RowDefinitions>
                         <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <views:PullToRefreshListView IsVisible="{Binding IsBusy, Converter={StaticResource oppositeConverter}}" Grid.Row="0" HasUnevenRows="true" ItemsSource="{Binding Sessions}" IsRefreshing="{Binding IsBusy}" RefreshCommand="{Binding RefreshCommand}" Message="Loading..."  RowHeight="120" >
                         <ListView.ItemTemplate>
                              <DataTemplate>
                                   <views:EventarinCell>
                                        <CustomCell:SessionDetailContentView />
                                   </views:EventarinCell>
                              </DataTemplate>
                         </ListView.ItemTemplate>
                    </views:PullToRefreshListView>
               </Grid>                        
          </StackLayout>
          
          
          
     </ContentPage.Content>
</pages:BaseContentPage>

